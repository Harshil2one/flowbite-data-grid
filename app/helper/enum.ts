export enum Align {
  Left = "left",
  Center = "center",
  Right = "right",
}

export enum Direction {
  Asc = "asc",
  Desc = "desc",
}

export enum PublicPath {
  Home = "/",
  Users = "/users",
}
